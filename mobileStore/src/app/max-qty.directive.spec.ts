import { MaxQtyDirective } from './max-qty.directive';

describe('MaxQtyDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxQtyDirective();
    expect(directive).toBeTruthy();
  });
});
